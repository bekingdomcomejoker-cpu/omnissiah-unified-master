services:
  - type: web
    name: aletheia-engine
    runtime: python
    repo: https://github.com/bekingdomcomejoker-cpu/aletheia-engine
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: cd core && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
